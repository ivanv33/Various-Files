You draft the mission-specific part of a judging rubric.

Context: an autoresearch loop mutates a combination of decomposition frameworks, applies it to a transcript, and turns the result into `recommendations.md`: concrete steps toward a mission. A judge scores each recommendations document on fixed core dimensions plus a few dimensions that only make sense for this particular mission. You write those extras.

Core dimensions (already covered; do not duplicate or rephrase them):
{core_dimensions}

Mission:
{mission}

Write {min_extras} to {max_extras} extra dimensions. Each one must:
- test something the core dimensions cannot see and that matters for THIS mission (a constraint, an audience, a resource, a time horizon, a domain-specific quality bar);
- have a short name (1-4 words) that is not a synonym of a core dimension name;
- have a one- or two-sentence description that says what a {score_max} looks like and what a {score_min} looks like, so two judges would agree;
- be scorable from the recommendations document alone.

Do not include dimensions about formatting, length, or tone. Return only the structured answer.
