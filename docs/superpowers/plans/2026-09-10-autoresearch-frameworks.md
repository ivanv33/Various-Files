# Autoresearch over Frameworks — Implementation Plan

> **For agentic workers:** this plan is deliberately high level. Implementers work Markov-chain style: read the state (this file's Progress log, `git log`, the tree under `autoresearch/`, the spec), pick the next milestone piece that fits your budget, TDD it, verify, commit, append a Progress log entry. Protocol: `autoresearch/dev/IMPLEMENTER.md`.

**Goal:** ship the engine described in `docs/superpowers/specs/2026-09-10-autoresearch-frameworks-design.md` (the contract) so `autoresearch_session` runs end to end under `langgraph dev` against a real session branch, and is ready for `langgraph deploy`.

**Architecture:** LangGraph Functional API (`@entrypoint` / `@task`) loop in `autoresearch/engine/loop.py`; creative steps are `deepagents` deep agents over a `FilesystemBackend` rooted at a fresh git clone; judge is a structured-output call; every attempt is committed and pushed to the session branch, which is the only memory across experiments and containers.

**Tech Stack:** Python 3.13, `uv`, langgraph 1.2.11, deepagents 0.7.13, langchain 1.4.0, langchain-google-genai 4.4.0, pytest 9, langgraph-cli 0.4.31 (`langgraph dev`, `langgraph deploy`).

## Global constraints

- Spec is the contract. Deviations only where this plan says so (see "Refinements"). If you must deviate elsewhere, record it in the Progress log with a reason.
- Work only inside `autoresearch/` (plus this plan file). Never edit `.env`; never commit secrets. `.env` is gitignored; `.env.example` documents variables.
- All model calls go through `GEMINI_MODEL` (`gemini-3.8-flash`, verified live) via `init_chat_model(f"google_genai:{GEMINI_MODEL}")`. Same model for every role.
- Every side-effecting step in the loop is a `@task` (checkpointed, replayed from cache on resume); `ensure_workspace` is a plain function. Task arguments and return values must be serializable (strings, dicts, pydantic models). Pass `ws.root` and `branch` as strings, not a live object.
- Agents never run git. `commit_push` stages only the session-folder allowlist: `experiments.tsv`, `notes.md`, `proposals/`, `best/`, `rubric.md`, `catalog.json`. `attempts/` is never staged.
- Tests must not need network or Gemini unless marked `@pytest.mark.live`. Default `pytest` run = offline, temp git repos, fake model.
- One commit per finished unit of work, message `<area>: <what>` + trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## Verified facts (do not re-derive)

- `gemini-3.8-flash` exists for the provided key; plain and `with_structured_output(PydanticModel)` calls work (~1–2 s). `AIMessage.content` is a **list of content blocks** for this model; use `msg.text` for plain text.
- Functional API resume (tested with `MemorySaver`): on `Command(resume=...)` the entrypoint body re-runs from the top, plain functions execute again, completed `@task`s return cached results without running, and a second `interrupt()` later in the body pauses again. Exactly the semantics the spec assumes.
- Cached `@task` results are matched by **task name + call order** (the PUSH write index) within one entrypoint run, and `interrupt()` values by call order too — not by arguments. Verified by `tests/test_loop.py`: across three replays every agent step ran exactly once. Hence refinement 8.
- `deepagents.create_deep_agent(model=..., backend=FilesystemBackend(root_dir=...), system_prompt=..., subagents=[SubAgent(...)])` with Gemini works: agent used `read_file` / `write_file` and wrote the requested file (4 s). `FilesystemBackend(root_dir, virtual_mode=True)` presents paths as **virtual absolute paths anchored at root_dir** (`/autoresearch/sessions/<slug>/...` maps to `<root>/autoresearch/sessions/<slug>/...`); `..` traversal is blocked. `SubAgent` is a TypedDict: `name`, `description`, optional `system_prompt`, `tools`, `model`, `mode`.
- `langgraph.json` keys accepted by CLI 0.4.31 include `python_version`, `dependencies`, `graphs`, `env`, `dockerfile_lines`. Local package: `"dependencies": ["."]` with `pyproject.toml` at `autoresearch/` (already scaffolded).
- Seed frameworks: 22 READMEs at `decomposition-frameworks/<category>/<slug>/README.md` on `origin/claude/decomposition-frameworks-66ea98`; `decomposition-frameworks/_meta/frameworks.json` on the same branch already has `slug`, `name`, `category`, `blurb` (use as `summary`), `slots`, `relations`. Read them with `git show origin/claude/decomposition-frameworks-66ea98:<path>`; do not check the branch out.
- Shortest transcript for smoke tests: `tbpn-transcripts/transcripts/2025-10-25_diet-tbpn-october-24th-2025.md` (~2,000 words). Full transcripts are ~50k words (fits Gemini's 1M context).
- Toolchain: `autoresearch/.venv` (uv, Python 3.13) already has all pinned deps. Run everything as `autoresearch/.venv/bin/python -m pytest`, `autoresearch/.venv/bin/langgraph dev`. Do not use the anaconda `langgraph` (0.3.6).

## Refinements to the spec (decided; implement these)

1. **Seed is pushed before the checkpoint-0 pause.** Order: `draft_rubric_extras` → `propose(seed)` writes `best/combination-with-explanations.md` → `commit_push("checkpoint 0 draft: seed combination")` → `interrupt(...)` → `apply_steer` (a `@task`: writes `rubric.md`, appends steer) → `commit_push("checkpoint 0: rubric approved")`. Checkpoint-0 detection stays `not exists(rubric.md)`.
2. **Step results carry content.** Agent steps return `{"path": <repo-relative>, "content": <str>}`. Before using a prior step's file, the loop calls `ws.materialize(result)` (write the cached content if the file is missing). This makes a re-invoked run after a container crash self-healing.
3. **Remote and workdir from env.** `GIT_REMOTE` (required), `AUTORESEARCH_WORKDIR` (default `/tmp/autoresearch-ws`); clone path `<workdir>/<branch with / replaced by __>` for the loop; the reviewer graph gets its own clone at `<that>--review` (`ensure_workspace(branch, role="review")`), because it may run while the loop is mid-experiment. `ensure_workspace`: clone `--single-branch --branch <branch> --depth 50` if missing, else `fetch` + `checkout -B <branch> origin/<branch>` (hard) and `git clean -fd` (attempts/ is scratch; losing it is fine because interrupts only happen between experiments). A `user:token@` in `GIT_REMOTE` is stripped from the URL and sent per git command as an `Authorization` header (`git_ops.split_remote`); no clone ever stores it, and deep agents are denied `/.git`.
4. **Local origin for dev.** For tests and `langgraph dev` smoke, origin is a local bare repo (`git clone --bare` of this worktree, `GIT_REMOTE=file:///...`). `scripts/new_session.py` works against `GIT_REMOTE` (clone master shallow, branch, write files, commit, push), so the same script serves local and GitHub.
5. **Push conflicts.** On rejected push: `git pull --rebase`; if the only conflict is `experiments.tsv`, resolve as union of data rows sorted by `n` (dedupe on `n`), continue rebase, push once more; any other conflict raises.
6. **Deep-agent guardrails.** `recursion_limit` per step (propose 120, raised from 60 after live recursion-limit failures; decompose 150, recommend 80); a step that raises or leaves the output file missing/empty yields `kept=error` with the reason in `note`; the loop continues.
7. **Judge order record.** `score.json` includes `order: ["candidate","incumbent"]` or reversed, `judge_model`, `experiment`, per-dimension scores for both, totals, rationale.
8. **Replay determinism.** Because cached tasks and interrupts are matched by call order, every decision the entrypoint body makes must come out the same on each replay. The two facts that drive control flow — is `rubric.md` present, how many rows are in `experiments.tsv` — are read through a `read_state` `@task` (called once before checkpoint 0 and once per loop iteration), not straight from the clone, which `ensure_workspace` resets to origin where those facts have already moved on. Everything else (digest trend, judge incumbent, summary) reads the clone directly as the spec says.

## File map and interfaces (names later milestones rely on)

```
autoresearch/
  langgraph.json, pyproject.toml, .env.example          (done)
  engine/
    config.py        Settings from env: gemini_model, git_remote, workdir, author name/email. make_model() -> BaseChatModel
    workspace.py     Workspace(root, branch, slug): session_dir, metadata, has(rel), read(rel), write(rel, text),
                     n_experiments(), max_experiments, totals(), materialize(result), virtual(rel) -> "/autoresearch/sessions/<slug>/<rel>"
    loop.py          @entrypoint autoresearch_session({"branch"}) — plain control flow exactly as spec §3 + refinements
    review.py        @entrypoint review_proposals({"branch"})
    tasks/git_ops.py ensure_workspace(branch) -> Workspace (plain); pull(root); commit_push(root, branch, message) -> sha|None;
                     stage_allowlist(root, session_rel) ; revert_stray_changes(root, session_rel) -> list[str]; merge_experiments_tsv(ours, theirs) -> str
    tasks/log.py     LogRow model; append_row(root, session_rel, row); read_rows(...) -> list[LogRow]; digest(kind, **facts) -> dict for interrupt()
    tasks/rubric.py  CORE_DIMENSIONS (fixed list of {id,name,description,scale 1-10}); draft_extras(model, mission) -> list[dim]; render_rubric(core, extras) -> str; parse_rubric(text) -> list[dim]
    tasks/judge.py   Verdict model (per-dim scores for candidate & incumbent, totals, rationale, order); judge(model, rubric, mission, candidate, incumbent|None) -> Verdict (one repair retry)
    tasks/propose.py / decompose.py / recommend.py   run(model, ws, n, brief...) -> {"path","content"}; each builds its deep agent from prompts/<step>.md
    tasks/promote.py copy attempts/<n>/* -> best/, write best/score.json
    prompts/*.md     propose.md, decompose.md, framework_decomposer.md, recommend.md, judge.md, rubric_extras.md, review.md
    catalog/catalog.json   22 seed entries {slug,name,category,summary,when_to_use,source:"seed"}
  scripts/build_catalog.py   reads the frameworks branch via `git show`, writes engine/catalog/catalog.json
  scripts/new_session.py     <slug> --mission --transcript --max-experiments [--remote]
  tests/                     conftest with tmp bare origin + seeded repo fixture; fake chat model fixture
  dev/IMPLEMENTER.md         the implementer protocol; dev/smoke.sh  (starts langgraph dev, runs one session, resumes through interrupts)
```

Keep files small and single-purpose; split when one grows past ~300 lines.

## Milestones (pick the next unfinished piece; tick when its tests pass and it is committed)

- [x] **M1 Foundations** — `config.py`, `workspace.py`, `tasks/git_ops.py`, `tasks/log.py` with unit tests on temp bare repos: clone-or-reset, allowlist staging, stray revert, push, push-conflict union merge, tsv append/parse/n_experiments/totals.
- [x] **M2 Catalog + session bootstrap** — `scripts/build_catalog.py` (generate and commit `engine/catalog/catalog.json`, 22 entries, `when_to_use` distilled from each README's "What it decomposes" + "Failure modes"), `scripts/new_session.py` + tests (branch created from master on a temp origin, files present, tsv header, notes skeleton).
- [x] **M3 Rubric + judge** — `tasks/rubric.py` (core dims, extras drafting via structured output, render/parse), `tasks/judge.py` (Verdict schema, randomized order recorded, repair retry, no-incumbent path) with a fake model; one `live` test each.
- [x] **M4 Agent steps** — prompts + `propose` / `decompose` (with `framework-decomposer` subagent) / `recommend` / `promote`; unit tests use a stub agent factory; one `live` test on the short transcript that writes real `attempts/1/*`.
- [x] **M5 Loop** — `loop.py` per spec §3 + refinements; loop test with stubbed steps and `MemorySaver`: checkpoint-0 pause, seed committed before pause, stop, continue+steer, exp-1 no incumbent, keep/discard, one commit per attempt, cap. `langgraph dev` must start and list both graphs (M6 can stub `review.py` as a minimal entrypoint until M7).
- [x] **M6 Local end-to-end** — `dev/smoke.sh`: bare origin from this worktree, `new_session.py` on the short transcript with `max_experiments=2`, `langgraph dev --no-browser`, drive the run through the SDK (create thread, run, resume `continue` at each interrupt), assert commits landed on the bare origin and `best/score.json` exists. Real Gemini.
- [x] **M7 Reviewer** — `review.py` per spec §5 with tests (accept new-framework → catalog entry; accept rubric-change → rubric edited + notes line; frontmatter updated; commit).
- [x] **M8 Deploy readiness** — `langgraph build` (or `langgraph dockerfile`) succeeds locally; README in `autoresearch/` with run/deploy instructions; `langgraph deploy` smoke is owner-gated (needs GitHub PAT) — document the exact command and env instead of running it.
- [x] **M9 Observe and simplify** — after M1–M8 are done and verified, do 2–3 real runs of a session (short transcript, `max_experiments` 2–3, real Gemini), reading traces, commits, `experiments.tsv`, `notes.md` and the agents' outputs. Then simplify: remove anything over-engineered, unused, or that the observed behaviour shows is unnecessary (extra abstractions, dead options, redundant prompts/instructions, tests that only pin implementation details). Keep the spec's behaviour and the offline suite green; each simplification is its own commit with a one-line rationale in the Progress log. Update the definition of done: M9 complete with at least one observed run after the simplifications.
- [ ] **M10 `langgraph up` runtime for `/mission-lab`** (owner-requested 2026-09-11, after the definition of done) — Docker `langgraph up --port 8123 --wait`: durable Postgres checkpointer, pushes from inside the container (so `GIT_REMOTE` in `.env` must carry the token), rebuild after code changes, Studio `baseUrl=http://127.0.0.1:8123`. What is known is written as a TODO section in `.claude/skills/mission-lab/SKILL.md`; implement and live-test only on the owner's request, no Docker before that.

## Definition of done

`autoresearch/.venv/bin/python -m pytest -m "not live"` green; `dev/smoke.sh` completes two experiments against a local origin with real Gemini; `langgraph dev` serves both graphs; `langgraph build -t autoresearch:dev` succeeds; M9 complete with at least one observed run after the simplifications; all milestones ticked; Progress log has a final entry. **Fully met 2026-09-11** (closing entry below): the image build ran on this machine and the image imports both graph modules; only `langgraph deploy` (owner credentials, not part of this definition) remains.

## Progress log

Append one entry per implementer run (newest last): `### <date> — <milestone/piece>` then 3–8 lines: what was done, commit sha(s), test command + result, what is next, any open problem.

### 2026-09-10 — M1 Foundations (implementer #1)
- Done: `engine/config.py` (`Settings.from_env`, `load_env`, `make_model`), `engine/tasks/log.py` (`LogRow`, `parse_tsv`/`render_tsv`, `append_row`/`read_rows`, `digest`), `engine/workspace.py` (`Workspace(root, branch)`: `session_rel/session_dir/rel/path/attempt_dir/attempt_rel`, `has` = present **and non-empty**, `read/write`, `metadata/mission/transcript_path/max_experiments`, `validate()`, `rows/n_experiments/totals`, `materialize`, `virtual/virtual_repo`), `engine/tasks/git_ops.py` (`ensure_workspace`, `pull`, `stage_allowlist`, `revert_stray_changes`, `merge_experiments_tsv`, `commit_push`, `clone_path`, `GitError`). Tests: `tests/conftest.py` (hermetic git env, `origin` bare repo, `session_branch` seeded via `seed_session()`, `settings`, `fake_model`), `test_config.py`, `test_log.py`, `test_workspace.py`, `test_git_ops.py`.
- Commit: `5c4c344`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live" -q` → `36 passed` (~5 s; slowest test 0.9 s).
- Interface notes for later milestones: git_ops functions are **plain**; `loop.py` should wrap them with `langgraph.func.task(...)` (verified `task(commit_push)` yields a `_TaskFunction`). Pass `str(ws.root)` and `branch` into tasks and rebuild `Workspace(root, branch)` inside. `commit_push` also calls `revert_stray_changes`; call `revert_stray_changes` yourself before `append_log` if you want strays in the row's `note`. `attempts/` is neither staged nor reverted (scratch). Rebase conflict rule: only `experiments.tsv` may conflict; union by `n`, **local row wins** on duplicate `n`; anything else → `rebase --abort` + `GitError` (local commit kept for inspection). `ensure_workspace` discards local commits/edits and runs `git clean -fd`; it also (re)sets `remote.origin.url` from `GIT_REMOTE` and local `user.name/email`.
- Next (M2): `scripts/build_catalog.py` + `engine/catalog/catalog.json`; `scripts/new_session.py` should produce exactly what `tests/conftest.py::seed_session()` produces (metadata.json, `HEADER` tsv, `notes.md` with `## Insights` / `## Human steering`, `catalog.json` = seed catalog) — consider switching the fixture to call the script once it exists.
- Open: `fake_model` fixture is a `GenericFakeChatModel` (no `with_structured_output`); M3 will likely need a small custom fake that returns a pydantic instance. No deviations from the spec.

### 2026-09-10 — M2 Catalog + session bootstrap (implementer #2)
- Done: `scripts/build_catalog.py` (`extract_section`, `first_paragraph`, `failure_mode_titles`, `distill_when_to_use`, `build_entries(meta, read_readme)`, `build_catalog(repo, ref)`, CLI `main`; reads `_meta/frameworks.json` and the 22 READMEs from the frameworks branch via `git show`, never checks it out), `engine/catalog/__init__.py` (`CATALOG_PATH`, `load_seed_catalog()`) plus the committed `engine/catalog/catalog.json` (22 entries: `slug, name, category, summary, when_to_use, source="seed"`, in `frameworks.json` order), `scripts/new_session.py` (`create_session(slug, *, mission, transcript_path, max_experiments, remote, workdir=None, base="master", catalog=None, keep_clone=False) -> {"branch","session_rel","sha","remote"}`, `SessionError`, `NOTES_SKELETON`, `redact`, CLI `main`), `scripts/__init__.py`. `tests/conftest.py::seed_session()` now delegates to `create_session` so fixtures and real sessions cannot drift. Tests: `tests/test_build_catalog.py`, `tests/test_new_session.py`.
- Commit: `da88e96`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `62 passed in 14.47s`. Generator is idempotent (`.venv/bin/python -m scripts.build_catalog` twice → identical md5).
- Decision: `when_to_use` is distilled **deterministically** — first paragraph of "## What it decomposes" + the titles of the "### Failure modes" items, which come in three shapes across the 22 READMEs (bold bullets, plain bullets, a `| Failure | ... |` table; all three handled and tested). No model call, so the catalog is reproducible and tests stay offline. Entries are 78–209 words; if agents need terser text later, `build_entries(meta, read_readme)` is the seam for a model-based distiller.
- Notes: `create_session` refuses an existing remote branch, a missing/empty transcript on `base`, or an existing session folder **before** pushing; shallow-clones (`--depth 1`) into `<workdir>/new-session__<slug>` and removes the clone unless `--keep-clone`; CLI reads `GIT_REMOTE`/`AUTORESEARCH_WORKDIR` from env (`.env` loaded without override) and redacts `user:token@` from anything it prints. Both `.venv/bin/python -m scripts.new_session ...` and `.venv/bin/python scripts/new_session.py ...` work.
- Next (M3): `tasks/rubric.py` (CORE_DIMENSIONS, `draft_extras(model, mission)` via `with_structured_output`, `render_rubric`, `parse_rubric`) then `tasks/judge.py`. The `fake_model` fixture is a `GenericFakeChatModel` with no `with_structured_output`; add a small fake in `conftest.py` whose `with_structured_output(Schema)` returns a runnable that yields queued pydantic instances and can raise once (to exercise the judge's repair retry).
- Open: none; no deviations from the spec.

### 2026-09-10 — M3 part 1: rubric (implementer #2)
- Done: `engine/tasks/rubric.py` — `Dimension(id, name, description, scale="1-10", kind="core"|"extra")`, `CORE_DIMENSIONS` (5: `specificity`, `grounding`, `mission_fit`, `actionability`, `insight`), `SCORE_MIN/SCORE_MAX = 1/10`, `MIN_EXTRAS/MAX_EXTRAS = 1/3`, `ExtraDimension`/`RubricExtras` (structured-output schema: the model supplies name + description, ids are `slugify(name)`), `validate_extras`, `draft_extras(model, mission)` (one call, one repair turn carrying the validation or provider error, then `RubricError`), `render_rubric(core, extras) -> str`, `parse_rubric(text) -> list[Dimension]` (round-trips; only `### <id>: <name>` headings under `## Core dimensions` / `## Mission-specific dimensions` are parsed, so owner prose edits and extra sections are harmless). `engine/prompts/__init__.py::load_prompt(name)` + `engine/prompts/rubric_extras.md` (`str.format` placeholders; keep other braces out of prompt files). `tests/conftest.py::structured_fake` — factory for a model whose `with_structured_output(Schema)` yields queued pydantic outputs / raises queued exceptions and records `(schema, input)` per call. Tests: `tests/test_rubric.py` (12 offline + 1 `live`).
- Commit: `1507a2b`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `74 passed, 1 deselected in 14.20s`; `.venv/bin/python -m pytest tests/test_rubric.py -m live` → `1 passed in 6.27s` (Gemini drafted 1–3 non-core extras that round-tripped through render/parse).
- Next (M3 part 2): `engine/tasks/judge.py` — `judge(model, rubric: str | Sequence[Dimension], mission, candidate, incumbent | None, *, experiment=None, judge_model="", rng=None) -> Verdict`. Present the documents as "Document A" / "Document B" in random order (`order` records which is which), have the model return per-dimension `a`/`b` integer scores plus a rationale, validate (every rubric id exactly once, ints in `[SCORE_MIN, SCORE_MAX]`, `b` present iff incumbent), one repair retry like `draft_extras`, map back to `Verdict{experiment, judge_model, order, candidate, incumbent|None, candidate_total, incumbent_total|None, rationale}` (this is what `promote` dumps into `best/score.json`, refinement 7). `prompts/judge.md`. Offline tests with `structured_fake`; one `live` test.
- Open: none.

### 2026-09-10 — M3 part 2: judge (implementer #2) — M3 complete
- Done: `engine/tasks/judge.py` — `DocScore(dimension_id, a, b|None)` / `JudgeAnswer(scores, rationale)` (what the model returns), `Verdict(experiment, judge_model, order, candidate: dict[id,int], incumbent: dict|None, candidate_total, incumbent_total|None, rationale)` with `.kept` property (strictly better; always kept when there is no incumbent) — `Verdict.model_dump(mode="json")` is exactly the `best/score.json` shape of refinement 7 — and `judge(model, rubric: str | Sequence[Dimension], mission, candidate, incumbent=None, *, experiment=None, judge_model="", rng=None) -> Verdict`. Documents are shown as `## Document A` / `## Document B` in random order (`rng.random() < 0.5` → candidate first) and mapped back through `order`; the system prompt (`engine/prompts/judge.md`) never contains the literal labels, so the no-incumbent path shows exactly one document. Validation: every rubric id exactly once, integer scores in `[SCORE_MIN, SCORE_MAX]`, `b` present iff an incumbent was given; one repair turn carrying the error text, then `JudgeError`. Tests: `tests/test_judge.py` (13 offline + 1 `live`).
- Commits: `91ce5b2` (judge), `1507a2b` (rubric, part 1).
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `87 passed, 2 deselected in 14.23s`; `.venv/bin/python -m pytest tests/test_judge.py -m live` → `1 passed in 3.25s` (Gemini scored the specific, grounded document above the vague one; all five core dims in range).
- Interface notes for M4/M5: in the loop, `dims = parse_rubric(ws.read("rubric.md"))` once per experiment, then `judge(model, dims, ws.mission, candidate_text, incumbent_text_or_None, experiment=n, judge_model=settings.gemini_model)`; wrap the call in `langgraph.func.task` and pass **strings** (rubric text, document texts), returning `verdict.model_dump(mode="json")` so the cached result is serializable — rebuild with `Verdict.model_validate(...)` if needed. `JudgeError` → log row `kept="error"` with the message in `note`. At checkpoint 0: `extras = draft_extras(model, ws.mission)`; the digest can carry `render_rubric(CORE_DIMENSIONS, extras)`; `apply_steer` writes that text to `rubric.md`. Both `draft_extras` and `judge` take any object with `with_structured_output(Schema)`; tests use `structured_fake`.
- Next (M4): `engine/prompts/{propose,decompose,framework_decomposer,recommend}.md`, `engine/tasks/{propose,decompose,recommend,promote}.py` with `run(model, ws, n, ...) -> {"path","content"}` and a stub agent factory for offline tests; `promote(ws, n, verdict)` copies `attempts/<n>/*` to `best/` and writes `best/score.json` from `Verdict.model_dump(mode="json")`. Verified facts on `deepagents` / `FilesystemBackend(virtual_mode=True)` are in the "Verified facts" section; `Workspace.virtual(name)` gives the agent-visible path. `engine/prompts/__init__.py::load_prompt` already exists; prompts use `str.format`, so keep other braces out of prompt files (or escape them).
- Open: none; no deviations from the spec.

### 2026-09-10 — M4 Agent steps (implementer #3) — M4 complete
- Done: `engine/tasks/steps.py` (`LIMITS = {"propose": 60, "decompose": 150, "recommend": 80}`; `default_agent_factory(*, model, root, system_prompt, subagents)` → `create_deep_agent(model=..., backend=FilesystemBackend(root_dir=root, virtual_mode=True), system_prompt=..., subagents=..., checkpointer=False)`; `brief_header(ws)`, `log_tail(ws)`, `run_step(model, ws, *, prompt_name, prompt_vars, brief, output_rel, recursion_limit, subagents=None, agent_factory=None)`, `finish`, `failure`), `engine/tasks/propose.py` (`COMBINATION_NAME`, `SEED_REL = "best/combination-with-explanations.md"`, `INSIGHTS_LINE_BUDGET = 40`, `parse_combination(text) -> (frameworks, note)`, `run(model, ws, n, *, seed=False, agent_factory=None, recursion_limit=60)`, `use_seed(ws, n)`), `engine/tasks/decompose.py` (`framework_decomposer_subagent()` SubAgent dict sharing the backend; `run(model, ws, n, combination, ...)`), `engine/tasks/recommend.py` (`run(model, ws, n, decomposition, ...)`), `engine/tasks/promote.py` (`promote(ws, n, verdict: Verdict | dict) -> list[str]` replaces `best/` wholesale with every file in `attempts/<n>/` plus `score.json` = `Verdict.model_dump(mode="json")`; `PromoteError` if the three judged files are missing, nothing half-written), prompts `propose.md` (`{insights_budget}`), `decompose.md` (`{subagent}`), `framework_decomposer.md`, `recommend.md`. Tests: `tests/test_steps.py` (17 offline with a `StubFactory` standing in for `create_deep_agent`, + 1 `live`), `tests/test_promote.py` (4).
- Commit: `b9b9e54`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `108 passed, 3 deselected in 13.05s`; `.venv/bin/python -m pytest tests/test_steps.py -m live` → `1 passed in 329.59s (0:05:29)`: one full experiment (seed propose → `use_seed` → decompose with three subagent fan-outs → recommend) on the 2,002-word transcript. Gemini chose `mece+dialectical-decomposition+issue-hypothesis-trees` (all catalog slugs, frontmatter parsed), wrote three `attempts/1/decomposition-<slug>.md` (19–29 KB) and a 41 KB merged `decomposition.md` ending in `## Cross-framework synthesis`, an 11 KB `recommendations.md` with `Why:`/`Non-obvious:` lines citing transcript line numbers, and rewrote `## Insights` in `notes.md` leaving `## Human steering` untouched; no proposal written (optional).
- Step result contract (decided; an implementation of refinement 6 that extends refinement 2): every step returns `{"path": <repo-relative>, "content": str, "error": str | None}` and **never raises** for agent or model failures (recursion limit, provider error, tool error, missing/empty output) — the one-line reason (≤ 600 chars, `ExceptionType: message`) is in `error`, so the `@task` result is still cached on resume and the loop maps it to a row with `kept="error"`, `note=error`. `propose.run` / `use_seed` add `"frameworks": list[str]` and `"note": str` parsed from the file's frontmatter (`frameworks: [a, b]` / `a + b` / dash list; `note:` optionally quoted); a combination with no `frameworks:` is an error result (content kept). `decompose.run` / `recommend.run` return their own error result when the upstream result has `error` or empty content, and call `ws.materialize(upstream)` before running the agent.
- Interface notes for M5: checkpoint 0 → `propose.run(model, ws, None, seed=True)` (writes `best/combination-with-explanations.md`); experiment 1 → `propose.use_seed(ws, 1)` (plain copy, no model call), later `propose.run(model, ws, n)`; then `decompose.run(model, ws, n, combo)`, `recommend.run(model, ws, n, decomp)`, `judge(model, ws.read("rubric.md"), ws.mission, recs["content"], ws.read("best/recommendations.md") if ws.has("best/recommendations.md") else None, experiment=n, judge_model=settings.gemini_model)`, `promote(ws, n, verdict)`. Row: `LogRow(n=n, frameworks=combo["frameworks"], candidate_total=..., incumbent_total=..., kept=verdict.kept, note=combo["note"])`, or `kept="error"`, `note=<first error among combo/decomp/recs/judge>`. Wrap each step in `langgraph.func.task`, passing `str(ws.root)`, `ws.branch`, `n` and the plain result dicts; rebuild `Workspace(root, branch)` and `make_model()` inside the task (a chat model is not a serializable task argument; constructing it is cheap). Deep agents are compiled with `checkpointer=False`, so none of their state is persisted under the loop's thread. Steps are git-free; `commit_push` after `append_log` stages `best/`, `notes.md`, `experiments.tsv`, `proposals/` as before.
- Timing: one experiment ≈ 5.5 min with `gemini-3.8-flash` on the short transcript (decompose dominates); budget ≥ 12 min for M6's `max_experiments=2` smoke plus judge calls. Note `promote` copies **every** file in `attempts/<n>/` (spec: `attempts/<n>/*`), so a kept attempt also commits the per-framework `decomposition-<slug>.md` files (~70 KB); restrict to `promote.REQUIRED` if `best/` gets too noisy.
- Next (M5): `engine/loop.py` per spec §3 + refinements 1–2 (seed pushed before the checkpoint-0 `interrupt`; `apply_steer` as a task writing `rubric.md` via `render_rubric(CORE_DIMENSIONS, extras)` and appending steer), plus a minimal `engine/review.py` entrypoint stub so `langgraph dev` lists both graphs. Loop test with `MemorySaver`, `origin` / `session_branch` fixtures, a stub `agent_factory` (copy `tests/test_steps.py::StubFactory`, or inject a `steps` module double), and `structured_fake` for the judge: checkpoint-0 pause, seed on origin before the pause, `stop`, `continue` + steer, exp-1 no incumbent, keep/discard, one commit per attempt, cap. Then the `langgraph dev` check from `dev/IMPLEMENTER.md` §4 (`/ok` and `/assistants/search` listing both graphs).
- Open: none.

### 2026-09-10 — M5 Loop (implementer #4) — M5 complete
- Done: `engine/loop.py` — tasks `read_state`, `draft_rubric_extras`, `propose_step`, `use_seed_step`, `decompose_step`, `recommend_step`, `judge_step` (returns `{"verdict": dump | None, "error": str | None}`, never raises; skips the model when upstream errored), `promote_step` (materializes combo/decomp/recs first, so it also heals after a restart), `append_log` (calls `revert_stray_changes`, appends `reverted stray changes: …` to `note`), `apply_steer` (writes `rubric.md` at checkpoint 0 only; inserts `- <utc>: <steer>` at the end of `## Human steering`, creating the section if missing), `pull_step`, `commit_push_step`; pure helpers `add_steering`, `parse_reply`, `first_error`, `experiment_row`, `commit_subject`, `summary`; body `run_session(inputs)` exactly as spec 3 + refinements 1, 2, 8; `build_graph(checkpointer=None)`; module-level `autoresearch_session = build_graph()` (no checkpointer — the platform supplies one; tests pass `MemorySaver()`). Commit subjects: `checkpoint 0 draft: seed combination`, `checkpoint 0: rubric approved`, `exp <n>: KEEP|discard <cand> vs <inc|->`, `exp <n>: ERROR <reason>`, `steer after exp <n>` (no commit when there was nothing to steer). Interrupt payloads: `digest("checkpoint0", branch, rubric, seed, sha)` and `digest("new_best", branch, n, verdict, trend, sha)`; resume `{"action": "continue"|"stop", "steer": str}` (a bare string is taken as the action; anything but `stop` continues). Summary: `{branch, head, attempts, kept, best_total, best_experiment, stopped: "checkpoint0"|"stop"|"max_experiments"}`. `engine/review.py` — read-only `review_proposals` stub: `ensure_workspace`, `list_proposals(ws) -> (open, decided)` from `status:` frontmatter, returns `{branch, head, open, decided, note}`; M7 replaces the body. `engine/tasks/promote.py` — copies only `REQUIRED` + `score.json` (orchestrator fix; `decomposition-<slug>.md` stays in `attempts/`). Tests: `tests/test_loop.py` (5; scripted agent factory via `monkeypatch.setattr(steps, "default_agent_factory", …)`, `structured_fake` as the model via `monkeypatch.setattr(loop, "make_model", …)`, fixed rng via `monkeypatch.setattr(judge, "random", …)` — no test seams in production code), `tests/test_review.py` (2), `tests/test_promote.py` updated.
- Commit: `07b8615`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `115 passed, 3 deselected in 21.03s`. Boot check: `.venv/bin/langgraph dev --no-browser --port 2024` → `curl -s localhost:2024/ok` → `{"ok":true}`; `POST /assistants/search` → graph ids `['autoresearch_session', 'review_proposals']`; server killed afterwards.
- Deviation: refinement 8 (control-flow facts via the `read_state` task) — without it the checkpoint-0 block is skipped on the replay after the first `new_best` pause and every later cached task and interrupt shifts by one. Otherwise the spec's flow is implemented literally.
- Next (M6): `dev/smoke.sh` — `git clone --bare` this worktree to a temp dir, `GIT_REMOTE=file://<bare>` and `AUTORESEARCH_WORKDIR=<tmp>` in the environment (keep `.env` for the API key/model), `scripts/new_session.py smoke --mission … --transcript tbpn-transcripts/transcripts/2025-10-25_diet-tbpn-october-24th-2025.md --max-experiments 2`, `langgraph dev --no-browser --port 2024`, then drive via `langgraph_sdk.get_sync_client(url=…)`: `t = client.threads.create()`, `r = client.runs.wait(t["thread_id"], "autoresearch_session", input={"branch": "autoresearch/smoke"})`, while `"__interrupt__" in r`: `r = client.runs.wait(t["thread_id"], "autoresearch_session", command={"resume": {"action": "continue"}})`; assert `git -C <bare> log autoresearch/smoke` has two `exp n:` commits and `best/score.json` exists on the branch. Budget ≥ 12 min of Gemini time (≈ 5.5 min per experiment plus judge). Note `langgraph dev` reads `.env` via `langgraph.json`; exported `GIT_REMOTE` / `AUTORESEARCH_WORKDIR` must win over `.env` (langgraph-cli loads `.env` without override), so `unset`/export them before starting the server, or write a temp `.env` and point a temp `langgraph.json` at it.
- Open: none.

### 2026-09-10 — M6 Local end-to-end smoke (implementer #5) — M6 complete
- Done: `dev/smoke.py` (`SmokeError`; `smoke_config(base, env)` — the real `langgraph.json` with `env` replaced by an inline mapping, refusing any key that looks like a secret; `drive(client, branch, *, max_resumes, log) -> (summary, interrupts)` — create thread, `runs.wait(input={"branch"})`, then `runs.wait(command={"resume": {"action": "continue"}})` at every `__interrupt__`; `check_origin(bare, branch, session_rel, expected, *, allow_errors) -> facts` — one `exp n:` commit and one tsv row per experiment, no error rows unless allowed, nothing from `attempts/`, `rubric.md` + `best/score.json` present; `start_server`/`wait_for_server`/`stop_server` (own process group, SIGTERM then SIGKILL), `report`, CLI `main` with `--slug --mission --transcript --max-experiments --port --root --allow-errors --max-resumes --boot-timeout`), `dev/smoke.sh` (thin wrapper: `cd autoresearch && .venv/bin/python -m dev.smoke "$@"`), `dev/__init__.py`, `tests/test_smoke_driver.py` (8 offline: config rendering + secret refusal, resume loop with a fake client, origin checks on a temp bare repo). Also, per owner instruction, milestone M9 + the DoD clause.
- Commits: `c8666af` (plan: M9), `2e9d8ba` (M6), `7b62432` (report lists only session commits; inert `disable_persistence` key removed).
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `123 passed, 3 deselected in 25.02s`.
- Smoke (real Gemini, `dev/smoke.sh --max-experiments 2`, 03:33–03:44 UTC 2026-09-11): exit 0 in 678 s. Origin `file:///tmp/autoresearch-smoke/origin.git`, branch `autoresearch/smoke`: `session smoke: bootstrap` → `checkpoint 0 draft: seed combination` (25043b4, 58 s) → `checkpoint 0: rubric approved` → `exp 1: KEEP 64 vs -` (b915f11, +215 s; seed `wardley-mapping+rumelt-strategy-kernel`, 7 rubric dims incl. extras `asymmetric_founder_leverage`, `macro_shift_arbitrage`) → `exp 2: KEEP 63 vs 57` (886725f, +403 s; `wardley-mapping+inversion-premortem+rumelt-strategy-kernel`). Three interrupts (checkpoint0, new_best ×2) resumed with `continue`; summary `{attempts: 2, kept: 2, best_total: 63, best_experiment: 2, stopped: "max_experiments"}`; `best/` holds exactly the four spec files (decomposition 49 KB, recommendations 15 KB); no `attempts/` file committed; no `steer after` commit for an empty steer; 140 Gemini calls; server killed, port free. Scratch kept at `/tmp/autoresearch-smoke` (smoke.log, server.log, origin.git, ws/); wiped on the next run.
- Verified facts (new): (a) `langgraph dev` applies the config's `env` *over* the inherited environment (`langgraph_api.cli.run_server` → `patch_environment` sets unconditionally), so exporting `GIT_REMOTE` does not beat `.env`; the smoke therefore writes a temp `langgraph.json` whose `env` is the mapping `{GIT_REMOTE, AUTORESEARCH_WORKDIR}` and lets the server inherit secrets from the driver's `load_env()` — nothing secret touches disk. `dependencies: ["."]` resolves against the server's cwd (`autoresearch/`), so the temp config can live in the scratch root. (b) `disable_persistence` is dropped by `validate_config` in CLI 0.4.31; `.langgraph_api/` pickles cannot be turned off (gitignored, harmless). (c) `langgraph_sdk`'s default read timeout is 300 s — shorter than one experiment; the driver passes `httpx.Timeout(read=None)`. (d) `runs.wait` returns interrupts as `{"value": <digest>, ...}` items under `__interrupt__`.
- Observations for M9: judge re-grading swings — the exp-1 document scored 64 alone and 57 as incumbent beside exp 2's 63, so exp 2 was kept by 6 while the incumbent's own score moved 7; single-judge noise is comparable to the margins being decided. Timings on the 2,002-word transcript: checkpoint 0 ≈ 1 min, exp 1 ≈ 3.6 min (seed, no propose), exp 2 ≈ 6.7 min (propose + decompose over three frameworks + judge with incumbent). The propose agent wrote no `proposals/*.md` in either experiment. Deep-agent nodes appear as `langgraph_node=model|tools` in the server log, not as the loop's task names. No engine bug surfaced; no deviation from the spec.
- Next (M7): replace the `engine/review.py` stub per spec 5 — `ensure_workspace`; for each open `proposals/*.md` one structured-output call (`Decision(accept: bool, reason: str)` via `with_structured_output`, prompt `prompts/review.md` given mission, rubric, catalog, tsv tail); accept `new-framework-*` → append `{slug, name, summary, when_to_use, source: "proposal:<file>"}` to the session `catalog.json`; accept `rubric-change-*` → edit `rubric.md` and add a timestamped line under `## Rubric changes` in `notes.md`; write `status` / `decision_reason` into the proposal frontmatter; `commit_push("review: <n> proposals")` (every side effect a `@task`). Tests with `structured_fake` on the `origin` / `session_branch` fixtures, extending `tests/test_review.py`; seed `proposals/` by hand in the fixture since the smoke produced none. Then M8 (`langgraph build -t autoresearch:dev`, README), then M9 with `dev/smoke.sh --max-experiments 3 --slug <fresh>` for the observed runs (LangSmith tracing was on; project from `.env`).
- Open: none.

### 2026-09-11 — M7 Reviewer (implementer #6) — M7 complete
- Done: `engine/tasks/proposals.py` — proposal-file helpers `parse_frontmatter` / `set_frontmatter` (replace-or-append `key: value`, one-line values, frontmatter created if missing), `proposal_status`, `proposal_kind(rel) -> (kind, slug)` from the file name (`new-framework-<slug>.md` / `rubric-change-<slug>.md`, else `unknown`), `list_proposals(ws) -> (open, decided)`, `add_rubric_change_note(notes, text, timestamp)` (list item under `## Rubric changes`, section created at the end of notes if missing); structured schemas `ProposedFramework{name, category, summary, when_to_use}`, `FrameworkDecision{accept, reason, entry|None}`, `RubricDecision{accept, reason, dimensions: list[ExtraDimension]|None}`; `precheck` (rejections that need no model: unknown kind, slug already in `catalog.json`, `rubric-change` before `rubric.md` exists); `validate_decision` (accept needs a complete entry, or 1–3 extras validated by `rubric.validate_extras` against the core parsed from the file); `decide(model, ws, rel) -> decision dict` (one call + one repair turn carrying the error, like `draft_extras`; never raises, `error` is set instead); `apply_decision(ws, decision)` (accept new-framework → append `{slug, name, category, summary, when_to_use, source: "proposal:<file>"}` to the session `catalog.json`; accept rubric-change → `rubric.md` = `render_rubric(<core as parsed>, <new extras>)` plus a timestamped `## Rubric changes` line naming the file and the new dimension ids; every decided proposal gets `status` + `decision_reason` in its frontmatter; an errored decision changes nothing). `engine/prompts/review.md` (`{mission} {rubric} {catalog} {log}`; the human turn carries file name, kind, slug and the proposal text). `engine/review.py` rewritten: tasks `decide_step`, `apply_step`, `commit_push_step`; body = `ensure_workspace` → for each open proposal in name order decide → apply → one commit `review: <n> proposal(s) (<a> accepted, <r> rejected[, <e> error(s)])` (no commit when nothing was open) → `{branch, head, sha, decisions: [{file, kind, status, reason}], accepted, rejected, errors, open, decided}`. Tests: `tests/test_review.py` (11 offline + 1 `live`).
- Commit: `957bf46`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `132 passed, 4 deselected in 30.78s`; `.venv/bin/python -m pytest tests/test_review.py -m live` → `1 passed in 9.34s` (Gemini returned valid `FrameworkDecision` and `RubricDecision` answers, including the nullable nested `entry` / `dimensions`, on a disk-only `Workspace`). Boot check: `.venv/bin/langgraph dev --no-browser --port 2024` → `curl -s localhost:2024/ok` → `{"ok":true}`; `POST /assistants/search` → `['autoresearch_session', 'review_proposals']`; server killed, port free.
- Decisions (inside spec 5, recorded because the spec leaves them open): (a) a rubric change can only replace the mission-specific extras (1–3, validated as at checkpoint 0); core dimensions, scale and total are fixed and the prompt tells the model to reject proposals that touch them; the re-render keeps the core descriptions as parsed from the session's `rubric.md`, so owner edits survive. (b) `source` is `proposal:<basename>`. (c) Deterministic rejections make no model call. (d) A decision that fails twice leaves the proposal `open`, counts under `errors`, and the other proposals are still applied and committed. (e) `decide` / `apply` are separate tasks per proposal, run sequentially, so a later proposal sees the catalog / rubric after the earlier one was applied.
- Notes for M9: `proposals.add_rubric_change_note` has the same shape as `loop.add_steering` (heading-scoped append) — a shared helper is a simplification candidate. `tests/test_review.py` copies four one-line origin helpers from `tests/test_loop.py` (`bare` / `subjects` / `show` / `head`); move them to `conftest.py` if a third test file needs them. No run has yet exercised the reviewer on a proposal the propose agent actually wrote (M6 produced none); when an M9 run produces one, invoke `review_proposals` on that branch and read the commit.
- Next (M8): `cd autoresearch && .venv/bin/langgraph build -t autoresearch:dev` (needs Docker running; `.venv/bin/langgraph dockerfile Dockerfile` is the no-Docker fallback to at least verify the generated Dockerfile), then `autoresearch/README.md`: run (`.env` from `.env.example`, `scripts/new_session.py`, `langgraph dev`, `dev/smoke.sh`, invoking `autoresearch_session` / `review_proposals` through the SDK with the resume payload), deploy (the spec 6 env table, the exact `langgraph deploy` command, owner-gated by the GitHub PAT — document, do not run). Then M9.
- Open: none; no deviations from the spec.

### 2026-09-11 — M8 Deploy readiness (implementer #7) — M8 complete
- Done: `autoresearch/README.md` (layout, setup with `uv`, the full env table with required/optional and local vs deploy values, run-locally steps: bare origin, `scripts/new_session.py`, `langgraph dev`, SDK snippet with `httpx.Timeout(read=None)` and the resume loop / payload, `review_proposals` invocation and return shape, commit-message vocabulary, `dev/smoke.sh`, tests, deploy section: image commands, deployment env/secrets table from spec 6, the exact owner-gated `langgraph deploy` command, deploy smoke per spec 8, operational notes from the M6 verified facts). `autoresearch/.dockerignore` (`.env`, `.env.*` except `.env.example`, `.venv/`, `.langgraph_api/`, `__pycache__/`, `.pytest_cache/`, `*.egg-info/`, `.git/`, `tests/`, `dev/`). `tests/test_langgraph_config.py` (4 offline: graph paths import to `Pregel` entrypoints; `python_version` 3.13 / `dependencies ["."]` / `env ".env"` / git in `dockerfile_lines`; `langgraph dockerfile` renders from the config with `FROM langchain/langgraph-api:3.13`, the git line and both graphs in `LANGSERVE_GRAPHS`; `.dockerignore` lists the five required entries).
- Commit: `836e8d7`.
- Test: `cd autoresearch && .venv/bin/python -m pytest -m "not live" -q` → 136 passed (132 baseline + 4).
- Verification: `docker info` → `failed to connect to the docker API at unix:///Users/poplar/.orbstack/run/docker.sock ... daemon running` (client 29.4.0, OrbStack context, daemon down; not started per the rules). Fallback `cd autoresearch && .venv/bin/langgraph dockerfile <scratch>/Dockerfile` → `✅ Configuration validated!` / `✅ Created: Dockerfile`, exit 0 in 0.7 s, no network; output = `FROM langchain/langgraph-api:3.13`, the `apt-get install ... git` line, `ADD . /deps/autoresearch`, `uv pip install ... -e .`, `ENV LANGSERVE_GRAPHS={"autoresearch_session": "/deps/autoresearch/engine/loop.py:autoresearch_session", "review_proposals": "/deps/autoresearch/engine/review.py:review_proposals"}`, `WORKDIR /deps/autoresearch`. Negative check: a config pointing a graph at a missing module makes the CLI raise `FileNotFoundError: Could not find local module` and exit non-zero, so the dockerfile test catches graph-path drift.
- Verified facts (new): (a) `langgraph dockerfile` works offline (no Docker, no network) and validates graph module paths against the config's directory. (b) The generated Dockerfile copies the whole config directory (`ADD . /deps/autoresearch`) with no `ENV` from `.env`; a plain `langgraph build` writes no `.dockerignore` (only `langgraph dockerfile --add-docker-compose` does), so without one `autoresearch/.env` and `.venv/` would enter the image. (c) CLI 0.4.31 `_ignore._build_ignore_spec` (used for the remote-build archive) merges built-in excludes (`__pycache__/ .git/ .venv/ venv/ node_modules/ .tox/ .mypy_cache/`) + `.dockerignore` + a `.gitignore` *in the same directory* — the repo's root `.gitignore` is not consulted for `autoresearch/`, so `.dockerignore` is the single guard for `.env` in both the local and the remote path. (d) `langgraph deploy` reads `--api-key` from `LANGSMITH_API_KEY` (or `LANGGRAPH_HOST_API_KEY` / `LANGCHAIN_API_KEY`) in the env or `.env`, defaults `--deployment-type dev`, builds remotely when Docker is unavailable, and accepts `--image <repo:tag>` to skip the build.
- Decisions: the generated Dockerfile is not committed (`langgraph build`/`deploy` regenerate it from `langgraph.json`; a checked-in copy would go stale). The DoD clause `langgraph build -t autoresearch:dev succeeds` is not yet demonstrated on this machine (daemon down) — owner-gated together with `langgraph deploy`; the README gives both commands. No spec deviation.
- Next (M9): observation runs with `dev/smoke.sh --slug <fresh> --max-experiments 2` (or 3), reading `experiments.tsv`, `notes.md`, `best/score.json`, `proposals/` on the bare origin and the server log; if a run writes a proposal, start `langgraph dev` against that origin and invoke `review_proposals` on the branch. Then the simplifications listed in the M6/M7 entries (shared heading-scoped append helper; origin test helpers into `conftest.py`) plus whatever the runs show is unused. When the owner has Docker running: `cd autoresearch && .venv/bin/langgraph build -t autoresearch:dev` closes the remaining DoD clause.
- Open: none.

### 2026-09-11 — M9 observation run 1 (implementer #7) — M9 in progress, no simplifications yet
- Run: `dev/smoke.sh --slug obs1 --max-experiments 2` (real Gemini, tracing on, 04:11:43–04:25:08 UTC) → `smoke OK`, exit 0 in 801 s; server killed (SIGTERM), 0 errors / tracebacks in `server.log`, 162 Gemini HTTP calls (140 in M6), one harmless google-genai warning (`Direct use of automatic function calling (AFC) ... not recommended`). Origin `file:///tmp/autoresearch-smoke/origin.git`, branch `autoresearch/obs1`, head `cecd4a9`: `session obs1: bootstrap` → `checkpoint 0 draft: seed combination` (6ea9204, 98 s; seed `wardley-mapping+systems-thinking+rumelt-strategy-kernel`; extras `early_stage_resource_realism`, `flow_exploitation`) → `checkpoint 0: rubric approved` → `exp 1: KEEP 63 vs -` (755025c, +265 s) → `exp 2: KEEP 63 vs 58` (cecd4a9, +436 s; `wardley-mapping+dialectical-decomposition+systems-thinking+rumelt-strategy-kernel`). Three interrupts resumed with `continue`; summary `{attempts: 2, kept: 2, best_total: 63, best_experiment: 2, stopped: "max_experiments"}`; the branch holds exactly the nine spec files (`best/` 4, `catalog.json`, `experiments.tsv`, `metadata.json`, `notes.md`, `rubric.md`); nothing from `attempts/`; no `steer after` commit for the empty steer. Sizes: combination 15.7 KB, decomposition 52 KB, recommendations 14.3 KB.
- Observations: (a) **Judge compression and incumbent down-grading, second time.** `best/score.json` exp 2: candidate 9 on all seven dimensions (63), incumbent 8/9/9/8/8/8/8 (58), `order: [candidate, incumbent]`; the same document scored 63 alone in exp 1. M6 saw 64 → 57. Two runs, four experiments, 4/4 kept, every keep by 5–7 points while the incumbent's own score fell by 5–7 — indistinguishable from "the judge prefers whichever is A / newer". Before trusting `kept`, M9 should read `order` across several experiments and consider a cheap control (grade the incumbent against itself, or repeat the judge call with the order flipped and average) — a spec 4.2 question for the owner, not a silent change. (b) **No proposals in 4 experiments across two runs**, so `review_proposals` is still unexercised on real agent output; either the propose prompt's proposal instruction is inert in practice or proposals are genuinely rare on a 2,000-word transcript. Decide in M9 whether to keep the instruction as is, nudge it, or test the reviewer on a hand-written proposal on a real branch. (c) `notes.md` `## Insights` is maintained (8 bullets, ~200–400 chars each, one bullet cites exp 1's per-dimension scores, i.e. the agent reads the log) but the bullets are long; check the line budget the prompt states against what the agent actually writes. (d) Timings on the 2,002-word transcript: checkpoint 0 98 s, exp 1 265 s (seed, no propose), exp 2 436 s (propose + 4-framework decompose + judge with incumbent); a 3-experiment run will take ~20 min, so budget one run per implementer. (e) The engine behaved exactly as in M6: no bug, no spec deviation; nothing to fix before simplifying.
- Simplification candidates (carried + new; none applied yet): shared heading-scoped append helper for `loop.add_steering` / `proposals.add_rubric_change_note`; move the four origin helpers duplicated in `tests/test_review.py` / `tests/test_loop.py` into `conftest.py`; check whether `dev/smoke.py` options nobody uses (`--max-resumes`, `--boot-timeout`) earn their keep; any prompt text the traces show is ignored (a LangSmith pass over the `obs1` run under project from `.env`).
- Next: apply the simplifications one commit each (a `langgraph dev` boot check after any `loop.py` / `review.py` change), then one more observed run (`--slug obs2 --max-experiments 2` or 3) and tick M9 if it passes. Owner-gated: `langgraph build -t autoresearch:dev` (Docker daemon) and `langgraph deploy` per `autoresearch/README.md`.
- Open: the judge-bias question in (a) needs an owner decision before any change to spec 4.2.

### 2026-09-11 — M9 judge calibration + simplifications (implementer #8; entry reconstructed by implementer #9 from the commits — #8's session ended before it wrote one)
- Done, one simplification per commit (04:42–04:45 UTC): `13e4c91` judge: prompt calibrated — scale anchors (10 rare … 1–2 absent), a procedure that lists every document's concrete deficiencies with quoted evidence *before* any score, "Document A/B are unlabeled positions", no reward for length / headings / dollar figures / tone, identical documents must get identical scores; `JudgeAnswer` gains `deficiencies_a` / `deficiencies_b` ahead of `scores` (field order is the grading order) and a missing second deficiency list is a repair-turn error; two `live` position-bias probes (same document in both positions; strong/vague pair in both orders) plus an offline pin that `loop.judge_step` passes no rng so the live path consults `judge.random` once per call (both orders occur). Rationale: obs1 and M6 both showed every candidate at 9/dimension while the re-graded incumbent fell 5–7 points (obs1 entry, observation a). `0b2479a` tests: the origin inspection helpers (`bare` / `subjects` / `show` / `head`, `BOOTSTRAP`) live once in `conftest.py` instead of copies in `test_loop.py` and `test_review.py`. `94032d1` engine: one heading-scoped `notes.md` append (`tasks/notes.py: append_under_heading`) behind `loop.add_steering` and `proposals.add_rubric_change_note`, with `tests/test_notes.py`. `f9003a9` dev: dropped the `--max-resumes` / `--boot-timeout` smoke flags whose defaults were never changed in three runs (`BOOT_TIMEOUT_S` constant).
- Tests (re-run by #9 on `f9003a9`): `cd autoresearch && .venv/bin/python -m pytest -m "not live" -q` → `142 passed, 6 deselected in 35.46s`. Live probes `.venv/bin/python -m pytest -m live -k "both_positions or both_orders" -s` → `2 passed in 28.34s`: same document as candidate and incumbent → 36/36 (`order=[candidate, incumbent]`) and 39/39 (`order=[incumbent, candidate]`), per-dimension scores identical within each call, 3-point drift between calls; strong vs vague → 44 vs 11 and 43 vs 11 across the two orders (vague document at 2–3 per dimension, so the anchors bite). `loop.py` was touched (`94032d1`); the boot check is covered by the obs2 smoke below (`graphs: ['autoresearch_session', 'review_proposals']`).
- Then started `dev/smoke.sh --slug obs2 --max-experiments 2` at 04:45:37 UTC; the session ended 28 s later (driver logged `interrupted`, server SIGTERM'd, origin held only `session obs2: bootstrap`). Handed to #9.
- Open: obs2 not yet observed.

### 2026-09-11 — M9 observation run 2 + smoke isolation (implementer #9) — M9 complete
- Recovered #8's handoff: its obs2 died 28 s in; re-ran `dev/smoke.sh --slug obs2 --max-experiments 2` (real Gemini, tracing on, 04:49:00–05:01:16 UTC) → `smoke OK`, exit 0 in 733 s; `server.log`: 0 tracebacks, 0 error-level lines, 158 Gemini calls, the one known AFC warning; server stopped, port 2024 free. Origin `autoresearch/obs2` head `470baa9`: `session obs2: bootstrap` (d37c55b) → `checkpoint 0 draft: seed combination` twice (b82112d, 6b4cd2d — see the replay note) → `checkpoint 0: rubric approved` (bdd9a97, 133 s) → `exp 1: KEEP 60 vs -` (354f400, +211 s) → `exp 2: KEEP 61 vs 56` (470baa9, +388 s). Seed `wardley-mapping+rumelt-strategy-kernel`; extras `ecosystem_flow_exploitation`, `consensus_trap_avoidance`; exp 2 `wardley-mapping+first-principles+rumelt-strategy-kernel`. Nine spec files on the branch, nothing from `attempts/`; summary `{attempts: 2, kept: 2, best_total: 61, best_experiment: 2, stopped: "max_experiments"}`. Sizes: combination 14.9 KB, decomposition 39.9 KB, recommendations 13.3 KB.
- Calibration effect (first run after `13e4c91`): exp 1 candidate 8/9/8/8/9/9/9 = 60 (obs1: seven 9s = 63), rationale naming three quotable deficiencies (step 4's $10–25k/month cloud-savings assumption, step 6's Ohio AI-personhood detour, step 5's generic playbook); exp 2 `order: [candidate, incumbent]`, candidate 8/9/8/9/9/9/9 = 61 vs incumbent 8/9/7/7/8/8/9 = 56 — no uniform 9s, both documents get 8 on `specificity`, and the exp-2 rationale for Document B repeats exactly the exp-1 deficiencies, so the judge finds the same faults both times. The incumbent still re-grades below its solo score (60 → 56, −4; obs1 −5, M6 −7), the size of the call-to-call drift the same-document live probe shows (36 vs 39 for one document, identical scores within each call), not position bias. The keep margin (5) is about that drift. Owner question (spec 4.2) stays open but is now bounded: accept as is, require a minimum margin, or judge twice and average.
- New observation — persisted-run replay: the first `checkpoint 0 draft` commit (b82112d) was made by #8's killed run. `langgraph dev`'s in-memory runtime pickles runs/threads/checkpoints to `.langgraph_api/` under its **cwd** (`autoresearch/`) and loads them on boot (`langgraph_runtime_inmem/database.py: start_pool` loads the ops file unconditionally; `LANGGRAPH_DISABLE_FILE_PERSISTENCE=true` only stops checkpoint/store persistence), so the fresh server first executed #8's pending run (thread `01a08ec9-2042…`, same branch name) against the new origin — clone, seed draft, push, interrupt — and ours then re-did checkpoint 0 (`rubric.md` still absent) and continued normally. For the engine this is the container-restart case behaving as designed (no crash, converged, one extra draft commit); it was only a smoke-hygiene problem.
- Fix: `fe25631` dev: the smoke's server runs from the wiped scratch root with `dependencies` / `graphs` anchored at `autoresearch/` by absolute path (`langgraph dev` resolves both against its cwd, `langgraph_cli/cli.py: dev`), so `.langgraph_api/` lands in `<root>` and a killed earlier smoke cannot replay into the next one; the developer's `autoresearch/.langgraph_api/` is left alone. Two offline tests replace the old `smoke_config` test. Boot check with the new config from a scratch cwd on port 2025: `server up ... graphs: ['autoresearch_session', 'review_proposals']`, `booted in 1.0s`, `GET /ok -> {'ok': True}`, `POST /assistants/search -> ['autoresearch_session', 'review_proposals']`, `POST /threads/search -> []` (the stale threads are not loaded), `.langgraph_api` created under the scratch cwd; stopped, port free. Corrects the M6 note "no way to turn that off": the directory follows the cwd.
- Other observations: still no proposals (0 in 6 experiments over 3 runs) — `review_proposals` stays verified offline (M7) and served, unexercised on real agent output; `notes.md` `## Insights` has 7 bullets of 180–326 chars, within the propose prompt's `{insights_budget}` line cap, so obs1 candidate (c) closes without a change. Timings: checkpoint 0 at 133 s (82 s stale replay + 51 s ours), exp 1 211 s, exp 2 388 s.
- Tests: `cd autoresearch && .venv/bin/python -m pytest -m "not live" -q` → `143 passed, 6 deselected in 35.68s`.
- Commits: `fe25631` (dev), then `plan: progress after M9`.
- M9 ticked: simplifications applied (`13e4c91`, `0b2479a`, `94032d1`, `f9003a9`, `fe25631`) and one observed run (obs2) after the engine-side ones. Definition of done: offline suite green; `dev/smoke.sh` completes two experiments against a local origin with real Gemini; `langgraph dev` serves both graphs; M9 complete; all milestones ticked. Owner-gated remainder: `cd autoresearch && .venv/bin/langgraph build -t autoresearch:dev` (Docker daemon down here: `docker info` → `failed to connect to the docker API at unix:///Users/poplar/.orbstack/run/docker.sock`), then `langgraph deploy` per `autoresearch/README.md` (LangSmith key, GitHub PAT). Nothing else is left for an implementer.
- Open: the judge drift / keep-margin decision (spec 4.2) for the owner; obs2 was observed after the engine simplifications but before `fe25631`, a dev-only change covered by the boot check and the offline tests.

### 2026-09-11 — Local image build (closing run, implementer #10) — definition of done fully met
- Environment: `docker info` → daemon up (client 29.4.0, OrbStack context; already running, so not started or quit by this run). Build: `cd autoresearch && .venv/bin/langgraph build -t autoresearch:dev` → exit 0 (05:05–05:07 UTC): pulled `langchain/langgraph-api:3.13` (13 layers), then 16 BuildKit steps all `DONE`, `naming to docker.io/library/autoresearch:dev`. The local-dependency step (`uv pip install --system --no-cache-dir -c /api/constraints.txt -e .`) resolved 57 packages and installed 22 in 3.0 s: `deepagents==0.7.13`, `langchain==1.4.0`, `langchain-google-genai==4.4.0`, `python-dotenv==1.2.3`, and upgrades over the base image `langgraph 1.2.6 → 1.2.11`, `langchain-core 1.4.8 → 1.6.2`, `langsmith 0.8.18 → 0.12.4` (the constraints file accepts the pyproject pins; no conflict). Image `autoresearch:dev`: 723 MB (`docker image ls`), `ENTRYPOINT ["/storage/entrypoint.sh"]`, `WORKDIR /deps/autoresearch`, `LANGSERVE_GRAPHS` = both graphs under `/deps/autoresearch/engine/`.
- Verification: `docker run --rm --entrypoint python autoresearch:dev -c "import engine.loop, engine.review; print('graphs import ok')"` → `graphs import ok`. `--entrypoint` is required: without it the arguments go to the base image's server start, which dies on `KeyError: "Config 'REDIS_URI' is missing, and has no default."` (that traceback is the server, not the import). `docker run --rm --entrypoint sh autoresearch:dev -c 'ls -a /deps/autoresearch'` → `.dockerignore .env.example README.md autoresearch_engine.egg-info engine langgraph.json pyproject.toml scripts` — no `.env`, `.venv`, `.langgraph_api`, `tests/`, `dev/`; the egg-info is written in-image by the editable install (none exists on the host). No secret-bearing env var in the image (`env` shows only the base image's `GPG_KEY`).
- Found and fixed: the first image carried the host's `engine/**/__pycache__/*.cpython-313.pyc` and `scripts/__pycache__` (md5 of `engine/__pycache__/loop.cpython-313.pyc` identical host vs image) although `.dockerignore` listed `__pycache__/` and `*.pyc`. Docker's `.dockerignore` matches a bare pattern at the context root only (Go `filepath.Match`; `**/` is needed for any depth) — unlike the CLI's gitignore-style spec for the remote-build archive in M8 fact (c), which is why the root-level `.env` / `.venv/` / `tests/` / `dev/` guards worked and the nested ones did not. Not a secret leak. `78c1b86` deploy: `.dockerignore` now `**/__pycache__/`, `**/*.pyc`, `**/*.egg-info/` with a comment; `tests/test_langgraph_config.py::test_dockerignore_keeps_secrets_and_scratch_out_of_the_image` asserts the three `**/` patterns (red before the change, green after). Rebuilt in 7 s (base layer cached) → image `33721435a70a`, 723 MB; `find /deps` for `.env* .venv .langgraph_api __pycache__ *.pyc *.egg-info` returns only `.env.example` and the in-image egg-info; imports still ok.
- Tests: `cd autoresearch && .venv/bin/python -m pytest -m "not live"` → `143 passed, 6 deselected in 34.64s` (`addopts` already carries `-q`; adding a second `-q` on the command line hides the summary line).
- Verified facts (new): (a) `langgraph build` 0.4.31 passes the context to the local `docker build`, so Docker's own `.dockerignore` semantics apply there (root-anchored bare patterns); the pathspec/gitignore semantics of M8 fact (c) apply only to `langgraph deploy`'s remote archive. (b) The image's entrypoint is the server; use `--entrypoint python|sh` for ad-hoc checks. (c) A cached rebuild is ~7 s; the first build's cost is the base-image pull.
- Commits: `78c1b86` (deploy), then `plan: closing entry — local image build verified, DoD met`.
- Definition of done: fully met. Owner-gated and outside the definition: `langgraph deploy` per `autoresearch/README.md` (LangSmith key, GitHub PAT), optionally `--image` with `autoresearch:dev` pushed to a registry.
- Open: the judge drift / keep-margin decision (spec 4.2) for the owner, unchanged.

### 2026-09-11 — Review fix-ups (implementer #11; independent review of `b2c3043`)

- Six findings fixed, one commit each, TDD (each test seen red first):
  1. **BLOCKER, reviewer and loop shared one clone** — `clone_path(workdir, branch, role)` / `ensure_workspace(..., role=)`; the reviewer clones to `<branch>--review` (`review.REVIEW_ROLE`), the loop's path is unchanged. Tests: different roots for the same branch; the loop's `attempts/1/x.md` and its unfinished `notes.md` survive a reviewer `ensure_workspace`; `review_proposals` run mid-experiment commits only the proposal + catalog and leaves the loop clone untouched. `859f809`.
  2. **Token in `<clone>/.git/config`** — `git_ops.split_remote` strips `user:token@` from `GIT_REMOTE` and sends `http.extraHeader: Authorization: Basic …` per command through `GIT_CONFIG_COUNT/KEY_0/VALUE_0` (env instead of `-c` so the header is never in argv, `ps`, or our own error text); `remote.origin.url` is the tokenless URL; `GitError` redacts every message via `redact()` (moved from `scripts/new_session.py`, now also masks Basic-auth values); `new_session.py` uses the same split. Belt and braces: `default_agent_factory` passes `FilesystemPermission(operations=["read","write"], paths=["/.git","/.git/**"], mode="deny")` (deepagents 0.7.13 `create_deep_agent(permissions=…)`; subagents inherit). Tests: pure split/redact; `ensure_workspace` with a tokenised `https://x-access-token:SECRET@…` remote against a network-stubbed `_run` (clone rerouted to the file origin, fetch/push/pull stubbed) — `.git/config` has no `SECRET`, argv never carries it, the header rides in env; `pull`/`commit_push` read the credential from settings/env; a real deep agent driven by a fake tool-calling model gets `permission denied` for `read_file /.git/config`, `ls /.git`, `write_file /.git/hooks/…` while `metadata.json` stays readable. `798e6cb`.
  3. `parse_tsv` skips blank lines and header rows anywhere. `889bd24`.
  4. `propose.parse_combination` joins `note: >` / `note: |` block scalars (chomping indicators accepted, continuation lines with colons kept); `proposals._FRONTMATTER` tolerates blank lines before the first `---`, so `set_frontmatter` no longer prepends a second block. `0de2062`.
  5. Dead code: `steps.StepError`, `AgentFactory` + every `agent_factory=` kwarg (the one seam is `monkeypatch.setattr(steps, "default_agent_factory", …)`; `test_steps.py` has a `stub_factory` fixture), `make_model(**kwargs)`, `commit_push(session_rel=)`, `pull(branch=None)` (branch required), `promote(dict)`, `Workspace.__repr__` — grep confirmed no call sites. `64c1111`.
  6. Tests: judge failing after its repair retry → `kept=error` row with `JudgeError…`, commit `exp 1: ERROR JudgeError…` (≤ 80-char tail), loop continues to experiment 2 (its judge call and repair turn both counted); the reviewer/loop coexistence test landed with finding 1; the prompt-phrase-pinning judge test is replaced by the schema-order assertion (`deficiencies_a, deficiencies_b, scores`). `1b30f13`.
- Test: `cd autoresearch && LANGSMITH_TRACING=false .venv/bin/python -m pytest -m "not live"` → `154 passed, 6 deselected in 41.12s` (was 143).
- Boot check: `.venv/bin/langgraph dev --no-browser --port 2024` → `GET /ok -> {"ok":true}`; `POST /assistants/search` → `['autoresearch_session', 'review_proposals']`; server killed, no errors in its log.
- Notes: `GIT_CONFIG_*` needs git ≥ 2.31 (local 2.50.1; the deploy image's Debian git is newer). `.env` sets `LANGSMITH_TRACING=true`, and the offline suite's graph runs now hit LangSmith's monthly trace cap (harmless 429 noise) — run the suite with `LANGSMITH_TRACING=false`. One slip: a targeted pytest without `-m "not live"` started `test_steps_live_…` against Gemini and was killed after ~2 min; no code impact. Skipped nothing.
- Next: nothing open from the review. `langgraph deploy` remains owner-gated as before.

### 2026-09-11 — `/mission-lab` start skill (implementer #12; owner-requested, outside the definition of done)
- Owner requirement: no new Python script. The fuzzy inputs (which transcript, mission wording, slug, base ref, steering) are the agent's job inside a subagent; the deterministic steps are `git`, `langgraph dev`, `curl` against the local API. `.claude/skills/mission-lab/SKILL.md` (project skill, `87122c0`): gather → delegate with a self-contained brief → report links → status/resume/steer/stop → `langgraph up` TODO (M10). The skeleton is written with shell: `jq -n` for `metadata.json`, `printf` for the tsv header and `notes.md`, `cp` of `engine/catalog/catalog.json`; each byte-identical to `scripts/new_session.py` output (checked with `json.dumps(load_seed_catalog(), indent=2, ensure_ascii=False)`). Docs: README quick start, spec section 10.
- Live check (one real session, following the skill literally through a general-purpose subagent): `autoresearch/demo-diet-tbpn-oct24` from `origin/master`, short transcript, `max_experiments=3`. `GIT_REMOTE` was empty in `.env` and was set to the tokenless origin URL; the bootstrap push (`b445a92`) and the engine's own push from inside `langgraph dev` (`7534ed6 checkpoint 0 draft: seed combination`) both went through the owner's osxkeychain credential helper. Thread `01a08f1e-761b-7652-b01c-e4cf7cc1d2f0` reached the checkpoint-0 interrupt (`kind=checkpoint0`, seed `wardley-mapping+rumelt-strategy-kernel+pdca-deming`) about 2 min after the run started and was left paused with the server running (pid in `/tmp/mission-lab/dev.pid`, log `/tmp/mission-lab/dev.log`).
- Playbook corrections from the live check: (a) `origin/master` has no `autoresearch/`, so the seed catalog is copied from this checkout's engine to `/tmp/mission-lab/` before `git checkout -b` (re-verified: byte-identical after the checkout); (b) `--no-reload` on `langgraph dev`, the watcher was active (`changes detected` every 10 s, no restart observed) and a `.py` edit would restart the server under a paused run; (c) status reads the thread's `status` (`interrupted|busy|idle|error`) because a run object says `success` for a paused run; the state's interrupt digest lives at `.tasks[].interrupts[].value` (`kind, sha, seed, rubric, resume, branch`); threads are tagged `metadata.branch` at creation so `POST /threads/search {"metadata":{"branch":...}}` finds them among the stale threads `.langgraph_api/` reloads on boot; (d) `/tmp/mission-lab` and absolute paths are named explicitly in the brief; a Co-Authored-By trailer on the bootstrap commit is allowed. Verified facts: `POST /threads/<id>/runs` returns `status: pending` and does not block; the OpenAPI `RunCreateStateful.command` accepts `{"resume": ...}`; `langgraph up` has `-p/--port`, `--wait`, `--watch`, `--postgres-uri`, and there is no `langgraph down`.
- Housekeeping: two untracked leftovers of an earlier script-based attempt (`autoresearch/scripts/local_server.py`, `autoresearch/tests/test_mission_lab.py`, importing a nonexistent `scripts.mission_lab`) were moved out of the tree into the session scratchpad; they were never committed and would have broken pytest collection.
- Open for the owner: LangSmith ingest answers `429 Monthly unique traces usage limit exceeded`, so this run's traces do not land in `autoresearch-frameworks`; Studio's thread view still works against the local `baseUrl`. Next: M10 on request.
